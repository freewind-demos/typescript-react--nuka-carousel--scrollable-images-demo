TypeScript React "nuka-carousel" Scrollable Images Demo
=======================================================

可以让多张图片滚动显示。

```
npm install
npm run demo
```

It will open page on browser automatically.
